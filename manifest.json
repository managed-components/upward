{
  "name": "Upward",
  "description": "Upward is a recruiting tool!",
  "namespace": "upward",
  "icon": "assets/upward.svg",
  "categories": ["Recruiting"],
  "provides": ["events"],
  "allowCustomFields": true,
  "permissions": {
    "server_network_requests": {
      "description": "Upward uses serverside network requests to report data",
      "required": true
    }
  }
}
